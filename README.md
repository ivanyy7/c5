# Калькулятор

Браузерный калькулятор с модульными и E2E тестами.

## 🚀 Деплой на Vercel

Проект готов к деплою на Vercel. Для деплоя:

1. Установите Vercel CLI (если еще не установлен):
   ```powershell
   npm install -g vercel
   ```

2. Войдите в Vercel:
   ```powershell
   vercel login
   ```

3. Деплой проекта:
   ```powershell
   vercel
   ```

4. Для продакшн деплоя:
   ```powershell
   vercel --prod
   ```

Или подключите репозиторий GitHub к Vercel через веб-интерфейс на [vercel.com](https://vercel.com).

## 📦 Установка зависимостей

```powershell
npm install
# или
pnpm install
```

## 🧪 Запуск тестов

### Модульные тесты (Vitest):
```powershell
npm test
npm run test:watch
npm run test:ui
npm run test:coverage
```

### E2E тесты (Playwright):
```powershell
npm run test:e2e
npm run test:e2e:ui
npm run test:e2e:headed
```

## 🏗️ Структура проекта

```
├── calculator.html      # Основной HTML файл калькулятора
├── public/              # Статические файлы для деплоя
│   └── index.html      # Главная страница
├── src/                 # Исходный код TypeScript
│   └── Calculator.ts   # Класс Calculator
├── tests/               # Модульные тесты
│   └── Calculator.test.ts
├── e2e/                 # E2E тесты
│   └── calculator.spec.ts
├── vercel.json          # Конфигурация Vercel
└── package.json         # Зависимости и скрипты
```

## 🌐 Использование

Откройте `calculator.html` в браузере или запустите локальный сервер:

```powershell
npm start
```

Затем откройте http://localhost:3000/calculator.html

## 📝 Функции калькулятора

- ✅ Базовые математические операции (+, -, ×, /)
- ✅ Поддержка десятичных чисел
- ✅ Обработка ошибок (деление на ноль)
- ✅ Поддержка клавиатуры
- ✅ Очистка дисплея (C)
- ✅ Удаление последнего символа (⌫)

## 🧪 Тестирование

Проект включает:
- **43 модульных теста** (Vitest) - проверка логики Calculator
- **99 E2E тестов** (Playwright) - проверка работы в браузере

## 📄 Лицензия

ISC
